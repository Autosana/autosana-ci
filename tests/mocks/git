#!/usr/bin/env bash
# Mock git for testing entrypoint.sh

if [ "$MOCK_GIT_FAIL" = "1" ]; then
    exit 1
fi

if [[ "$*" == *"rev-parse HEAD"* ]]; then
    echo "${MOCK_GIT_SHA:-0123456789abcdef0123456789abcdef01234567}"
else
    command git "$@"
fi
